const debugError = (err: string) => {
  console.log(`🔥🔥 ERROR 🔥🔥: ${err}`);
};

const debugData = (data: string) => {
  console.log(`✅✅ Data ✅✅: ${data}`);
}

export default debugError;
